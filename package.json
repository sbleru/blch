{
  "name": "blch",
  "version": "1.0.0",
  "description": "BLEACH tldr utility",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf build && tsc && cp -r data build/",
    "mkmd": "rm -rf build/pages && cp -r pages build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "blch": "./build/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sbleru/blch.git"
  },
  "keywords": [
    "Git",
    "CLI"
  ],
  "author": "sbleru",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^13.7.4",
    "chalk": "^3.0.0",
    "commander": "^4.1.0",
    "csvtojson": "^2.0.10",
    "typescript": "^3.7.5"
  }
}
